sudo: required

services:
  - docker

before_install:
  - docker build -t avula810/docker-react -f Dockerfile.dev .

script:
  - docker run avula810/docker-react npm run test -- --coverage

deploy:
  provider: elasticbeanstack
  region: "us-east-1"
  app: "docker-react"
  env: "DockerReact-env"
  bucket_name: "elasticbeanstalk-us-east-1-377660074932"
  bucket_path: "docker-react"
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key:
    secure: "$AWS_SECRET_KEY"
